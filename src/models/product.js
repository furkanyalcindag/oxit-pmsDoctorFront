export default class Product {
    constructor(name, barcode_number, uuid, quantity, price) {

        this.name = name;
        this.barcode_number = barcode_number;
        this.uuid = uuid,
        this.quantity = quantity,
        this.price = price
    }
}
